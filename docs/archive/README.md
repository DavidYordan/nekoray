# 历史归档

此目录保存有历史价值但不再代表当前产品要求的材料：

- `audits/`：一次性调查和运行观察。
- `plans/`：agent 设计、实施计划和阶段清单。
- `contracts/`：已废弃或尚未成为现行规范的格式契约。

归档文档必须在标题后的第一段说明状态、归档日期、替代文档和已知错误。除非现行文档明确引用，归档中的 `[x]`、“用户已确认”或“已完成”都不能作为当前验收证据。

Git 历史本身足以保存纯上游 Linux 操作说明，因此这类无当前价值的文档已直接删除，没有复制到 archive。
